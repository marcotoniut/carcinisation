[package]
name = "process-gfx"
version = "0.1.0"
edition.workspace = true
description = "Converts source art to palette-reduced, rescaled textures for the game renderer"

[dependencies]
image = "0.25"
palette = "0.7.6"
serde = { version = "1.0", features = ["derive"] }
toml = "1.0"
