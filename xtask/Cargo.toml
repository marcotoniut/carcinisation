[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
cargo_metadata = "0.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "3", default-features = false, features = ["json"] }
