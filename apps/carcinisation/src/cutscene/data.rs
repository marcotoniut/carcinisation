//! Re-export from `carcinisation_cutscene::data`.
//! Types moved to the shared crate so the editor can depend on them
//! without pulling in the full application binary.

pub use carcinisation_cutscene::data::*;
