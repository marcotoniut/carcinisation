pre-commit:
  parallel: true
  commands:
    rustfmt-check:
      run: cargo fmt --all -- --check
    biome-lint:
      run: pnpm lint
    ruff-check:
      run: proto run ruff -- check

pre-push:
  commands:
    tests:
      run: ./bin/pre-push.sh
