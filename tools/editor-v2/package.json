{
  "name": "stage-editor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "gen-types": "cd ../.. && make gen-types",
    "watch-types": "cd ../.. && cargo watch --quiet --ignore \"bindings/*\" --ignore \"target/*\" --ignore \"*.ts\" -w apps/carcinisation/src --env QUIET=1 --env RUSTFLAGS=-Awarnings -s \"echo '🔄 Rust file changed, regenerating types...' && cargo run --quiet -p carcinisation --bin gen_types --features derive-ts 2>&1 | grep --line-buffered -E '(⚡|✅|⚠️|❌|🔄|error\\[|Error:|Failed)' || echo '❌ Type generation failed'\"",
    "dev": "concurrently --kill-others --names \"VITE,TGEN\" -c \"bgMagenta.bold,bgBlue.bold\" \"vite\" \"pnpm watch-types\"",
    "prebuild": "pnpm gen-types",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "dependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "zod": "4.1.12",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@types/react": "19.2.2",
    "@types/react-dom": "19.2.2",
    "@types/wicg-file-system-access": "2023.10.7",
    "@vitejs/plugin-react": "5.1.0",
    "concurrently": "^9.2.1",
    "ts-to-zod": "^5.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "7.1.12",
    "vitest": "4.0.3"
  }
}
